# 插件编写规范

本规范用于在 `tavern_helper_template` 仓库中编写酒馆助手脚本、前端界面，以及（如果需要）SillyTavern 原生扩展。它回答两个问题：**去哪里查 API**，以及**按什么流程把插件写出来并挂到角色卡上**。

与 `制卡规范.md` 的分工：内容创作、世界书、MVU 变量设计参考 `tavern-cards` skill；文件放置、构建和打包以本仓库规则为准。

## 先分清四层概念

- **酒馆原生 UI 扩展**：独立于角色卡运行的全局扩展，放在酒馆 `data/<用户>/extensions` 目录，用 `manifest.json` 声明入口。功能强大但安装门槛高，本仓库默认不写这种项目。
- **酒馆助手脚本**：`src/{项目}/index.ts`，没有自己的界面，在无沙盒 iframe 中后台运行。脚本里的 `$` 直接作用于整个酒馆页面。
- **酒馆助手前端界面**：`index.ts` + `index.html`，在消息楼层的 iframe 中渲染显示，适合状态栏、HUD、游戏化界面。
- **STscript / 快速回复**：酒馆自带的轻量脚本语言，不写完整插件也能实现简单自动化。详见 [STscript 文档](https://docs.sillytavern.app/usage/st-script/)。

## 文档入口（从哪里找 API）

- **酒馆助手官方文档**：[JS-Slash-Runner-Doc](https://n0vi028.github.io/JS-Slash-Runner-Doc/)，重点看“功能详情”下的分类：变量、楼层消息、事件、世界书、预设、角色卡、请求生成、分享接口、访问酒馆接口或其他插件、内置第三方库等。
- **本仓库 `@types/`**：酒馆助手接口的本地类型定义，是最权威的速查来源。`@types/function` 中的接口会导出到 `window.TavernHelper`；`@types/iframe` 中的接口只能在酒馆助手前端界面或脚本内使用。索引见 `.cursor/rules/酒馆助手接口.mdc`。
- **SillyTavern 官方文档**：[Writing Extensions](https://docs.sillytavern.app/for-contributors/writing-extensions/)（原生扩展）、[STscript](https://docs.sillytavern.app/usage/st-script/)。
- **运行时自查**：在酒馆页面按 F12，控制台输入 `window.TavernHelper` 或 `SillyTavern.getContext()` 查看当前环境实际提供的接口。

## API 速查

### 酒馆助手（TavernHelper / iframe 接口）

- `window.TavernHelper`：聊天消息、变量、世界书、预设、角色卡、人设、正则、导入导出数据、请求生成、音频、宏、注入提示词、slash 命令等稳定接口。文档中带 🚫TavernHelper 标记的函数未导出到 `window`，只能在脚本/界面 iframe 内直接调用。
- **事件**：`eventOn` / `eventOnce` / `eventEmit` / `eventEmitAndWait`；事件类型包括 `tavern_events`（近百种酒馆事件）、`iframe_events`（酒馆助手事件）和自定义字符串事件。监听会随 iframe 关闭自动卸载，不需要手动清理。
- **楼层消息**：`getCurrentMessageId`、`getChatMessages`、`formatAsDisplayedMessage`、`retrieveDisplayedMessage` 等（见 `@types/function/chat_message.d.ts`、`displayed_message.d.ts`）。
- **请求生成**：`generate`（使用当前/指定预设）和 `generateRaw`（自定义提示词顺序），支持流式（`iframe_events.STREAM_TOKEN_RECEIVED_*`）、`tools`、`json_schema`、`custom_api`，以及 `stopGenerationById` / `stopAllGeneration`。
- **注入提示词**：`injectPrompts` / `uninjectPrompts`。
- **变量**：`TavernHelper.getVariables` / `replaceVariables` / `updateVariablesWith` / `insertVariables` / `deleteVariable`。
- **宏**：`registerMacroLike`；工具函数 `substitudeMacros`、`errorCatched` 等。
- **跨脚本共享**：`initializeGlobal` 共享接口，其他脚本用 `await waitGlobalInitialized('名称')` 获取。
- **内置第三方库**：`$`（jQuery）、`_`（Lodash）、`z`（zod）、`Vue` / `VueRouter`、`YAML`、`toastr`、`showdown`、`PIXI`、FontAwesome、tailwindcss。

### 酒馆原生（SillyTavern）

- `SillyTavern.getContext()`：`chat`、`characters`、`characterId`、`groups`、`groupId`、`extensionSettings` + `saveSettingsDebounced`（扩展持久化设置）、`chatMetadata` + `saveMetadata`（聊天元数据）、`writeExtensionField`（写入角色卡扩展字段）、`renderExtensionTemplateAsync`（渲染 HTML 模板）等。
- 事件：`eventSource` 上的 `APP_INITIALIZED`、`APP_READY`、`CHAT_CHANGED`、`MESSAGE_RECEIVED` 等。**在酒馆助手脚本里优先用 `tavern_events` + `eventOn`，不要直接依赖 `eventSource`**。
- 其他插件接口：`EjsTemplate`（提示词模板）、`Mvu`（MVU 变量框架，使用前先 `await waitGlobalInitialized('Mvu')`）。
- **不要 import 酒馆内部模块路径**（如 `../../script.js`），内部结构随时会变；优先使用 `getContext()` 和 `TavernHelper` 提供的稳定接口。

## 在本仓库写插件的流程

1. **判断类型**：只有 `index.ts` 是脚本；`index.ts` + `index.html` 是前端界面。
2. **建目录**：
   - 角色卡内：`src/{角色卡名}/脚本/{脚本名}/index.ts` 或 `src/{角色卡名}/界面/{界面名}/index.ts` + `index.html`
   - 独立项目：`src/{项目名}/`，不套角色卡结构
   - 可参考 `初始模板/脚本/新建为src文件夹中的文件夹/`、`初始模板/前端界面/新建为src文件夹中的文件夹/`
3. **按既有约定写代码**：
   - 加载用 `$(() => {...})`，卸载用 `$(window).on('pagehide', ...)`
   - 脚本中 `$` 作用于酒馆页面；要挂 Vue 组件到酒馆页面时，用 `util/script.ts` 的 `createScriptIdDiv` + `app.mount($app[0])`；独立隔离界面用 `createScriptIdIframe`，样式用 `teleportStyle`（详见 `.cursor/rules/脚本.mdc`）
   - 前端界面 `index.html` 只写静态 `<body>`，样式和脚本由 webpack 内联；禁止用 `<script src>` / `<link href>` 引本地文件
   - 脚本设置用 zod 定义类型和默认值；按钮用 `replaceScriptButtons` / `appendInexistentScriptButtons` + `getButtonEvent` + `eventOn`
   - 可恢复错误用 `console.warn` / `console.error`；致命错误可抛出，并用 `errorCatched` 包裹顶层初始化
4. **构建与联调**：
   - `pnpm build`：生产构建，同时生成 schema.json 并触发 tavern_sync 打包
   - `pnpm watch`：开发模式，会启动酒馆监听服务（端口 6621）推送更新；本地调试时把 `初始模板/脚本/导入到酒馆中/脚本-实时修改.json` 导入酒馆助手，路径指向你本地静态服务器上的 `dist` 文件
   - 确认 `dist/{项目}/index.js` 或 `dist/{项目}/index.html` 已生成；正式使用的 CDN 地址用 `https://testingcf.jsdelivr.net/gh/...`，不要把 localhost 留在正式包里
5. **挂到角色卡**：
   - 脚本：在角色卡 `index.yaml` 的 `扩展字段.酒馆助手.脚本库` 中新增条目，内容为 `import 'https://testingcf.jsdelivr.net/gh/.../dist/{角色卡名}/脚本/{脚本名}/index.js'`
   - 状态栏界面：通过正则把 AI 输出的 `<StatusPlaceHolderImpl/>` 替换为 `$('body').load('https://.../dist/{角色卡名}/界面/状态栏/index.html')`（参考 `初始模板/角色卡/新建为src文件夹中的文件夹/index.yaml`）
   - 每新增一个脚本/界面，检查 `index.yaml` 是否引用它；未引用的文件不会进入最终角色卡

## 常见任务速查

| 想做什么 | 用什么 | 参考位置 |
| --- | --- | --- |
| 监听楼层更新/消息接收 | `eventOn(tavern_events.MESSAGE_UPDATED, ...)` | `示例/脚本示例/监听消息修改.ts` |
| 修改/创建/删除楼层消息 | `TavernHelper` 的 chat_message 接口 | `@types/function/chat_message.d.ts` |
| 后台请求 AI 生成 | `generate` / `generateRaw` | `@types/function/generate.d.ts`、[请求生成](https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/%E5%8A%9F%E8%83%BD%E8%AF%A6%E6%83%85/%E8%AF%B7%E6%B1%82%E7%94%9F%E6%88%90.html) |
| 给 AI 请求注入提示词 | `injectPrompts` / `uninjectPrompts` | `@types/function/inject.d.ts` |
| 读/写变量 | `TavernHelper` 的 variables 接口 | `@types/function/variables.d.ts` |
| 操作世界书 | `TavernHelper` 的 worldbook / lorebook 接口 | `@types/function/worldbook.d.ts`、`lorebook*.d.ts` |
| 给脚本加按钮 | `replaceScriptButtons` + `getButtonEvent` + `eventOn` | `示例/脚本示例/添加按钮和注册按钮事件.ts` |
| 弹提示 | `toastr.success/info/warning/error` | `示例/脚本示例/index.ts` |
| 做设置界面 | zod 定义 + Vue 组件 | `示例/脚本示例/settings.ts`、`设置界面.ts` |
| 与酒馆页面交互 | `$('body')`、`createScriptIdDiv`、`teleportStyle` | `.cursor/rules/脚本.mdc` |
| 让其他脚本调用你的接口 | `initializeGlobal` / `waitGlobalInitialized` | [分享接口](https://n0vi028.github.io/JS-Slash-Runner-Doc/guide/%E5%8A%9F%E8%83%BD%E8%AF%A6%E6%83%85/%E5%88%86%E4%BA%AB%E6%8E%A5%E5%8F%A3.html) |

## AI 生成与酒馆 API 连接

`generate` / `generateRaw` 使用酒馆「API 连接」中当前生效的连接，不绕过酒馆。玩家在酒馆里配置好连接后卡片即可使用；代码和文档中不要写死连接信息。

排查连接问题时，按以下顺序确认：

- **当前来源**：酒馆「API 连接」→ 聊天补全里的「聊天补全来源」下拉框决定实际使用哪一套配置（OpenAI 反代、自定义兼容、各服务商面板互相独立）。改错面板等于没改，先确认下拉框当前值。
- **保存生效**：修改输入框后要点该面板的「连接」按钮（或对应保存按钮）才会保存并写入内存；只改输入框不点保存，下次仍可能是旧配置。
- **反代预设**：使用 OpenAI 反代预设时，地址和密码要写进预设本身，并用预设编辑器的保存按钮保存；预设下拉框里选中的名称才是当前生效的预设。
- **URL 拼接**：不同来源对地址是否带 `/v1` 的处理不同；报 404 时先通过 F12 网络面板确认实际请求的完整地址，再调整地址写法。
- **模型名**：模型必须是当前服务商可用列表中的精确名称；报 “model not found” 时，用带 Key 的模型列表接口确认可用模型，再填到模型输入框。
- **错误码速判**：401/403 多为 Key 缺失、过期或无权限；404 多为地址或模型名不对；502/连接超时多为服务商侧问题。
- **验证连通性**：可以先在浏览器控制台用最小请求直接请求服务商接口（注意不要打印 Key），确认地址、Key、模型可用后，再回到酒馆界面填写。

界面类插件常见附加问题：

- 程序化创建消息（如 `createChatMessages`）后，酒馆助手不会自动把界面代码块包成 iframe，需要再调用 `setChatMessages([{ message_id: <新楼层> }], { refresh: 'affected' })` 重新渲染。
- 前端界面从 CDN 加载时可能命中旧缓存，加载地址应带时间戳参数（如 `?t=${Date.now()}`）。

## 安全

酒馆助手脚本拥有完整 JavaScript 权限，可以读取 API 密钥、聊天记录和修改酒馆设置。只安装和编写来源可信的脚本；写插件时保持最小权限，不要包含窃取凭据、发送未授权请求或破坏用户数据的代码。不要在代码或文档中写死 API 地址、Key 或模型名，这些应由玩家在酒馆「API 连接」中自行配置。

## 禁止事项

- 不把 `dist/` 生成产物手工修改后当作源码维护。
- 不 import 酒馆内部模块路径，不依赖未公开的内部全局变量。
- 不混用本仓库的 `index.yaml` / `tavern_sync` 打包与 `tavern-cards-forge` 的 `.cardrc.json` / `cards/` 结构。
- 不把脚本、界面和世界书内容混在一个文件里维护。
- 不为小修改重写整个脚本或角色卡目录。
- 不把 localhost、未部署的占位 CDN 留在正式角色卡中。
