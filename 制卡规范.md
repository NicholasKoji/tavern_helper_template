# 制卡规范

本规范用于在 `tavern_helper_template` 仓库中制作 SillyTavern 角色卡、世界书、MVU 变量、状态栏界面和相关酒馆助手脚本。

## 核心原则

- 内容创作、世界书结构、角色条目规划、MVU 变量设计可以参考 `tavern-cards` skill。
- 文件放置、前端开发、脚本开发和最终打包以本仓库规则为准。
- 不在本仓库内混用两套打包格式：本仓库使用 `index.yaml` / `tavern_sync.yaml` / `tavern_sync.mjs`；`tavern-cards-forge` 使用 `.cardrc.json` / `cards/{Project}/tavern-cards-state.json`。
- 如无明确要求，不运行 `tavern-cards-forge init`、`tavern-cards-forge pack` 或 `tavern-cards-forge unpack` 来管理本仓库项目。
- 修改已有角色卡时，先理解当前目录结构和已有条目，再增量修改，不重建、不搬家、不替换整套结构。

## 推荐目录结构

角色卡项目放在 `src/{角色卡名}/` 中，示例和模板可参考 `示例/角色卡示例/` 与 `初始模板/角色卡/新建为src文件夹中的文件夹/`。

常用结构如下：

```text
src/{角色卡名}/
  index.yaml
  schema.ts
  schema.json
  第一条消息/
    0.txt
  世界书/
    变量/
      initvar.yaml
      变量列表.txt
      变量更新规则.yaml
      变量输出格式.yaml
    角色/
    世界观/
    事件/
  脚本/
    变量结构/
      index.ts
    MVU/
      index.ts
  界面/
    状态栏/
      index.ts
      index.html
      App.vue
      store.ts
      global.css
      components/
```

如果只是写独立脚本或独立前端界面，不要套用完整角色卡结构，按 `初始模板/脚本` 或 `初始模板/前端界面` 新建单独目录即可。

## 角色卡内容编写流程

1. 明确角色卡定位：题材、主角关系、玩法循环、是否需要 MVU、是否需要状态栏、是否需要 EJS 或分阶段世界书。
2. 先写变量和世界观的骨架，再写具体角色条目，避免后期变量结构反复推倒。
3. 若使用 MVU，先完成 `schema.ts`，再据此编写 `世界书/变量/initvar.yaml`、`变量列表.txt`、`变量更新规则.yaml`、`变量输出格式.yaml`。
4. 世界书条目按功能拆分，优先保持信息密度和可维护性，不把所有设定塞进单个长条目。
5. 第一条消息放入 `第一条消息/0.txt`，如有多开场，按 `1.txt`、`2.txt` 继续追加，并同步更新 `index.yaml`。
6. 每次新增文件后检查 `index.yaml` 是否引用了它；未引用的世界书、脚本和界面不会进入最终角色卡。

## MVU 变量规范

- `schema.ts` 只负责导出 `Schema` 和类型，不写副作用代码。
- 使用 zod 4 写变量结构，默认可使用全局 `z` 和 `_`，不要在 `schema.ts` 中额外 import 它们。
- 数字优先使用 `z.coerce.number()`，需要范围限制时优先用 `.transform(value => _.clamp(value, min, max))`。
- 默认值优先使用 `.prefault()`，复杂对象的子字段也应有兼容的默认输入。
- 动态集合优先使用 `z.record()`，少用数组索引表达长期状态。
- 字段名前缀 `_` 表示只读或内部字段，变量更新规则中不要要求 AI 更新这类字段。
- 更新规则要描述“什么时候更新”和“更新幅度/格式”，不要重复解释字段名已经能说明的内容。

## 脚本规范

- 角色卡脚本放在 `脚本/{脚本名}/index.ts`。
- MVU 角色卡通常需要 `脚本/变量结构/index.ts` 注册 schema。
- 代码在酒馆环境中运行，优先使用本仓库 `@types` 中定义的酒馆助手接口。
- 加载时使用 `$(() => { ... })`，不要依赖 `DOMContentLoaded`。
- 卸载清理使用 `$(window).on('pagehide', ...)`。
- 可恢复错误用 `console.warn` 或 `console.error` 记录；会导致功能不可用的错误可以抛出，并用 `errorCatched` 包裹顶层初始化函数。

## 前端界面规范

- 前端界面目录必须同时包含 `index.ts` 和 `index.html`。
- `index.html` 只写静态 `<body>` 内容，不用 `<script src>` 或 `<link>` 引入本地文件。
- 样式优先写在 Vue 组件或由 `index.ts` import 的 CSS/SCSS 中。
- 状态栏界面优先放在 `界面/状态栏/`，通过 `defineMvuDataStore(Schema, { type: 'message', message_id: getCurrentMessageId() })` 读取楼层变量。
- 前端状态栏的正则只负责定位和替换占位符，例如 `<StatusPlaceHolderImpl/>`；变量读取和解析交给前端代码。
- 界面需要适配消息楼层 iframe，避免横向滚动，不使用受宿主高度影响明显的 `vh` 做主体高度。

## 打包与验证

常用命令：

```bash
pnpm build
pnpm watch
pnpm sync bundle all
```

`pnpm build` 会执行 Webpack 构建，并触发 schema 导出与 `tavern_sync` 打包。`pnpm watch` 用于本地实时开发和酒馆助手监听。

打包前检查：

- `schema.ts` 能正常生成 `schema.json`。
- `index.yaml` 中引用的文件都存在。
- `第一条消息`、`世界书`、`脚本`、`界面` 路径没有错别字。
- CDN 地址使用 `https://testingcf.jsdelivr.net/gh/...`，不要使用不可访问或未部署的占位链接。
- 状态栏开发阶段可临时加载 localhost，正式打包前必须改回可访问的部署地址。

## 与 tavern-cards Skill 的协作边界

可参考：

- 角色基础信息、性格调色盘、三面性、多阶段设定等写作方法。
- 世界观、NPC、事件、地理、文风指导等条目拆分方式。
- MVU 变量设计、变量更新规则、EJS 动态上下文思路。
- 角色卡质量评估和查漏补缺清单。

不直接采用：

- `.cardrc.json` 作为本仓库主配置。
- `cards/{Project}/tavern-cards-state.json` 作为本仓库角色卡数据源。
- `tavern-cards-forge pack` 作为本仓库默认打包方式。
- `tavern-cards-forge init` 自动创建出的目录结构，除非用户明确要求新建独立 forge 项目。

当 AI 同时看到本仓库规则和 `tavern-cards` skill 时，应遵循以下优先级：

1. 用户当前明确要求。
2. 本文件和本仓库 `.cursor/rules/` 中的项目规则。
3. `tavern-cards` skill 的内容创作方法。
4. 通用 SillyTavern 或前端开发经验。

## 禁止事项

- 不把生成产物手工塞进 `dist/` 后当作源码维护。
- 不在未确认用途时新增 `.cardrc.json`、`cards/` 或 `tavern-cards-state.json`。
- 不把状态栏 HTML、脚本和世界书内容混在一个文件里维护。
- 不为了小修改重写整个角色卡目录。
- 不删除 `示例/` 和 `初始模板/`，它们是后续开发的重要参考。
